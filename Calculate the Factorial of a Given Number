#include<stdio.h>

int main()
{
    int number, factorial = 1, i = 1;
    printf("enter number");
    scanf("%d", &number);
    while (i <= number)
    {
        factorial = factorial * i;
        i = i + 1;
    }
    printf("%d", factorial);
    return 0;
}
